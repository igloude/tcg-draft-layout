/**
 * Transition to the pack view. Called when a new pack is available for picking, and when the host starts a draft.
 */
var goToView = (viewName, draftId, playerId) => {
    
}

export default goToView;
